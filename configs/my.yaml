# PARITY CONFIG — DO NOT ENABLE SNIPER FILTERS / TRADING HOURS
app:
  name: "Divergence Sentinel (TV Parity)"
  log_level: "INFO"

provider:
  type: "binance"
  market: "futures"
  symbols:
  - BTCUSDT
  - ETHUSDT
  - SOLUSDT
  - PAXGUSDT
  - XAGUSDT

  timeframes: ["15m"]
  warmup_candles: 350
  rest_timeout_s: 30
  ws_heartbeat_s: 20
  warmup_concurrency: 3

strategy:
  mode: "tv_parity"
  parity_strict: true
  don_len: 120
  pivot_len: 5
  osc_len: 14
  ext_band_pct: 0.15

  long_only: true
  trade_enabled: true

  entry_wait_confirm: true
  use_bos_confirmation: false
  bos_atr_buffer: 0.10
  max_wait_bars: 30

  min_div_strength: 15.0
  cooldown_bars: 0

  use_cvd_gate: true
  cvd_len_min: 60
  use_dynamic_cvd_pct: true
  cvd_lookback_bars: 2880
  cvd_pct: 75
  cvd_threshold: 244.075
  use_close_minus_1ms: true

  ks_long_enabled: false
  ks_short_enabled: false
  min_score_to_trade: 0

trading_hours:
  enabled: false

telegram:
  enabled: true
  token: "8595351198:AAGfwVdX2mNiRlGk78uhLFOSUCWiyq648Cw"
  chat_ids: [5195322248]

alerts:
  parse_mode: "HTML"
  detail_level: "public"
  include_score_breakdown: false
  include_features: true
  dedupe: true

webhook:
  enabled: true
  url: "http://127.0.0.1:9001/tv"
  secret: "CHANGE_ME"
  include_tf: true
  timeout_s: 10
